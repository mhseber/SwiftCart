# SwiftCart - E-commerce Project

এটি একটি সিম্পল ই-কমার্স ওয়েবসাইট যেখানে ইউজার প্রোডাক্ট দেখতে পারে, ক্যাটাগরি অনুযায়ী ফিল্টার করতে পারে এবং কার্টে প্রোডাক্ট যোগ করে চেকআউট করতে পারে।

---

## 📝 প্রশ্ন ও উত্তর (Assignment Questions)

এখানে জাভাস্ক্রিপ্ট সম্পর্কিত কিছু গুরুত্বপূর্ণ প্রশ্নের উত্তর দেওয়া হলো:

### ১) null এবং undefined এর মধ্যে পার্থক্য কী?

**উত্তর:** null হলো এমন একটি ভ্যালু যা প্রোগ্রামার নিজে থেকে কোনো ভেরিয়েবলে সেট করে দেয় এটি বোঝাতে যে ভেরিয়েবলটি এখন খালি। অন্যদিকে, undefined মানে হলো একটি ভেরিয়েবল ডিক্লেয়ার করা হয়েছে কিন্তু সেটিতে এখনো কোনো মান (value) সেট করা হয়নি।

---

### ২) JavaScript-এ map() ফাংশনের কাজ কী? এটি forEach() থেকে কীভাবে আলাদা?

**উত্তর:** map() ফাংশন একটি অ্যারের প্রতিটি উপাদানের ওপর কাজ করে এবং ফলাফল হিসেবে একটি নতুন অ্যারে রিটার্ন করে। কিন্তু forEach() শুধু অ্যারের প্রতিটি উপাদানের ওপর লুপ চালায়, এটি নিজে থেকে কিছু রিটার্ন করে না। অর্থাৎ, যখন আমাদের রেজাল্ট হিসেবে নতুন একটি অ্যারে দরকার হয় তখন আমরা map() ব্যবহার করি।

---

### ৩) == এবং === এর মধ্যে পার্থক্য কী?

**উত্তর:** == (Double equal) শুধুমাত্র দুটি ভ্যালু বা মান সমান কি না তা চেক করে, কিন্তু তাদের ডাটা টাইপ চেক করে না। অন্যদিকে, === (Triple equal) মান এবং ডাটা টাইপ—উভয়ই সমান কি না তা কড়াভাবে চেক করে। তাই আধুনিক জাভাস্ক্রিপ্টে নির্ভুল কোড লেখার জন্য === ব্যবহার করাই সবচেয়ে ভালো অভ্যাস।

---

### ৪) API থেকে ডেটা ফেচ (Fetch) করার ক্ষেত্রে async/await এর গুরুত্ব কী?

**উত্তর:** এপিআই থেকে ডাটা আসতে কিছুটা সময় লাগে। জাভাস্ক্রিপ্ট সাধারণত একটি কাজ শেষ হওয়ার আগেই পরের লাইনে চলে যায়। async/await ব্যবহার করলে কোডটি ডাটা আসা পর্যন্ত অপেক্ষা করে এবং ডাটা আসার পরই পরের লাইনের কাজ শুরু করে। এতে কোড এলোমেলো হয় না এবং ডাটা লোড হওয়ার সময় কোনো এরর হওয়ার সম্ভাবনা কমে যায়।

---

### ৫) JavaScript-এ Scope এর ধারণা ব্যাখ্যা করুন (Global, Function, Block)।

**উত্তর:** Global Scope: কোনো ভেরিয়েবল যদি সবার উপরে ডিক্লেয়ার করা হয়, তবে তা কোডের যেকোনো জায়গা থেকে এক্সেস করা যায়।

Function Scope: কোনো ভেরিয়েবল একটি ফাংশনের ভেতর ডিক্লেয়ার করলে তা শুধুমাত্র ওই ফাংশনের ভেতরেই কাজ করবে।

Block Scope: let বা const দিয়ে কোনো ভেরিয়েবল যদি {} (সেকেন্ড ব্র্যাকেট) বা কোনো ব্লকের ভেতর লেখা হয়, তবে তা ওই ব্লকের বাইরে ব্যবহার করা যায় না।

---

## 🚀 ফিচারসমূহ (Features)

- ডাইনামিক প্রোডাক্ট ডিসপ্লে (FakeStore API ব্যবহার করে)
- ক্যাটাগরি ভিত্তিক ফিল্টারিং
- শপিং কার্ট (Sidebar Drawer)
- সুইট অ্যালার্ট (SweetAlert2) নোটিফিকেশন
- লোকাল স্টোরেজ ডাটা পারসিস্টেন্স

## 🛠️ টেকনোলজি ব্যবহৃত (Technologies Used)

- HTML5
- Tailwind CSS
- DaisyUI
- JavaScript (ES6+)
- SweetAlert2
